# 🎭 เจาะลึกระบบหน้าบ้านและหุ่นยนต์อวตาร (Frontend & Avatar Deep Dive)

เอกสารฉบับนี้อธิบายการทำงานของ "ร่างกาย" (Frontend) ที่ทำหน้าที่รับคำสั่งจากสมอง (Backend) มาแสดงผลให้ผู้ใช้เห็น ทั้งในรูปแบบ 3D Avatar, UI Chat, และ Interactive Widgets

---

## 1. เทคโนโลยีเบื้องหลัง (Frontend Stack)

*   **Core:** HTML5, CSS3, Vanilla JavaScript (ES6+)
*   **Animation Engine:** **GSAP (GreenSock)** - หัวใจสำคัญของการขยับโมเดล
*   **3D/Avatar Logic:** ใช้ CSS Transform 3D ร่วมกับ GSAP เพื่อสร้างความลึกและการเคลื่อนไหวที่ลื่นไหล (ไม่ใช่ WebGL เพื่อความเบา)
*   **Communication:** WebSocket (Real-time Audio/Events)

---

## 2. โครงสร้างไฟล์และหน้าที่ (`apps/web-cloud`)

*   `robot_avatar.html`: หน้าจอหลักที่มี Avatar ยืนอยู่
*   `assets/scripts/robot_avatar.js`: **ไฟล์พระเอก** ควบคุมทุกอย่างเกี่ยวกับหุ่นยนต์
*   `assets/scripts/chat.js`: ควบคุม Logic การ Chat, WebSocket, และการส่งเสียง
*   `assets/scripts/fab_manager.js`: ควบคุมปุ่มเมนูลอย (Floating Action Button)

---

## 3. เจาะลึก Logic การขยับตัวของ Avatar (`robot_avatar.js`)

การที่หุ่นยนต์ดู "มีชีวิต" เกิดจาก 3 ระบบย่อยทำงานประสานกัน:

### 3.1 The Idle Loop (การหายใจและกระพริบตา)
หุ่นยนต์ไม่ได้ยืนนิ่งๆ แต่มีการขยับเล็กน้อยตลอดเวลาเพื่อให้ดูเป็นธรรมชาติ
*   **Blinking:** ใช้ `setInterval` สุ่มเวลาทุก 4-6 วินาที สั่งให้ `scaleY` ของดวงตาเหลือ 0.1 ชั่วคราว (หลับตา)
*   **Breathing:** ใช้ CSS Animation (`@keyframes float`) ให้ตัวหุ่นขยับขึ้นลงเบาๆ ตลอดเวลา

### 3.2 Mouse Tracking (การมองตาม)
หุ่นยนต์จะหันหน้าและกลอกตาตามเมาส์ของผู้ใช้
*   **Event:** `mousemove`
*   **Logic:** คำนวณระยะห่างระหว่าง "จุดกึ่งกลางหน้า" กับ "เมาส์"
*   **Animation:** ใช้ `gsap.quickTo` (เพื่อ Performance ที่ดีกว่า `.to`) สั่งย้ายตำแหน่งตาดำ (`x`, `y`) ตามเมาส์ แต่มีการ **Clamp** (จำกัดระยะ) เพื่อไม่ให้ตาหลุดจากเบ้า

### 3.3 Emotional States (อารมณ์และการแสดงออก)
Avatar เปลี่ยน Class CSS เพื่อขยับแขนและเปลี่ยนสีหน้าตามสถานะ:
1.  **Normal:** ยืนปกติ มองตามเมาส์
2.  **Listening:** เอียงหูมาข้างหน้า (ขยับแขนซ้ายแนบหู) หยุดมองตามเมาส์ชั่วคราว
3.  **Thinking:** ยกมือจับคาง (ท่าครุ่นคิด) ดวงตาลอยขึ้นบน
4.  **Speaking:** ขยับแขนประกอบการพูด (Gesticulation)

---

## 4. โหมดนำเสนอ (Presentation Mode)

เมื่อ AI ต้องการโชว์ข้อมูล หุ่นยนต์จะ **"หลบฉาก"** ให้ข้อมูลเด่นกว่า

```javascript
enterPresentationMode(data) {
    // 1. สั่งหุ่นย้ายไปขวาสุด และย่อส่วนลง
    gsap.to(robotContainer, { x: '32vw', scale: 0.85 }); 
    
    // 2. เอาหน้าจอข้อมูล (Info Display) เลื่อนขึ้นมาแทนที่
    gsap.to(presentationArea, { opacity: 1, visibility: 'visible', y: 0 });
}
```

*   **Trigger:** เกิดขึ้นเมื่อ Backend ส่ง Event `SHOW_MAP_EMBED` หรือมี `image_url` ในคำตอบ
*   **Content:** รองรับ HTML, Google Maps Embedded, และ Image Gallery

---

## 5. การผนวกรวม FAB Widget (`fab_manager.js`)

ปุ่มเมนูลอย (Music, FAQ, Nav, Calc) ทำงานแยกเป็น Widget อิสระ แต่สามารถ "สั่งงาน" Avatar ได้

*   **On Click:** สร้าง Widget HTML (เช่น เครื่องคิดเลข)
*   **Display:** เรียกใช้ `displayWidgetInPresentation()` ของ Avatar เพื่อโชว์ Widget ในพื้นที่หน้าอกหุ่น
*   **Interaction:** เมื่อกดปุ่มใน Widget (เช่น กด "แนะนำที่เที่ยว" ใน FAQ Widget) มันจะยิงข้อความเข้า Chat Box และสั่ง Submit ให้อัตโนมัติเสมือนคนพิมพ์เอง

---

## สรุป Flow การทำงานหน้าบ้าน

1.  User ขยับเมาส์ -> Avatar มองตาม
2.  User กดไมค์พูด -> Avatar ทำท่า **Listening** -> ส่งเสียงไป Backend
3.  Backend คิด -> Avatar ทำท่า **Thinking**
4.  Backend ส่ง Text/Audio -> Avatar ทำท่า **Speaking** (ขยับปากตามเสียง) + เข้าสู่ **Presentation Mode** (ถ้ามีภาพ/แผนที่)
