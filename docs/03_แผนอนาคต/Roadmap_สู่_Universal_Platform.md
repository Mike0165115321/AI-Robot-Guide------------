# 🚀 Roadmap สู่ Universal AI-PR Platform (แผนการขยายระบบสู่อนาคต)

เอกสารฉบับนี้ร่างเส้นทางการพัฒนา (Technical Roadmap) เพื่อเปลี่ยน **"AI Robot Guide จังหวัดน่าน"** ให้กลายเป็น **"Universal AI Platform"** ที่ใครๆ ก็สามารถมาสร้างหุ่นยนต์ AI ของตัวเองได้

---

## 🌎 Vision: The "WordPress" for AI Avatars
เป้าหมายคือการทำระบบที่ผู้ประกอบการ (โรงแรม, ร้านอาหาร, พิพิธภัณฑ์) สามารถ:
1.  **Register:** สมัครสมาชิกและสร้าง AI Agent ของตัวเอง
2.  **Upload:** อัปโหลดข้อมูล PDF/Word ของร้านตัวเอง
3.  **Embed:** นำ Code Widget ไปแปะหน้าเว็บตัวเองได้ทันที หรือใช้ลิงก์ Chatbot สำเร็จรูป

---

## Phase 1: Separation of Concerns (แยก "น่าน" ออกจาก "ระบบ")
*สถานะ: กำลังดำเนินการ*

ปัจจุบันข้อมูล "จังหวัดน่าน" ฝังอยู่ใน `prompts/` และ `database` เราต้องแยกมันออกมาเป็น "Default Content" เท่านั้น

### 1.1 Configurable Prompts
*   **สิ่งที่ต้องทำ:** ย้าย System Prompt ที่ Hardcode ว่า *"คุณคือไกด์น่าน"* ไปไว้ใน Database
*   **New Implementation:** 
    ```python
    # เดิม: 
    system_prompt = "You are Nan Guide..."
    
    # ใหม่ (Phase 1):
    agent_config = db.agents.find_one({"agent_id": current_agent_id})
    system_prompt = agent_config["system_prompt"]
    ```

### 1.2 Tenant-Aware Vector Search
*   **สิ่งที่ต้องทำ:** เพิ่ม field `tenant_id` ลงใน Qdrant Payload และบังคับ filter ทุกครั้งที่ Search
*   **New Schema:**
    ```json
    {
      "id": "uuid...",
      "vector": [...],
      "payload": {
        "content": "...",
        "tenant_id": "shop_123",  // <-- สำคัญมาก
        "category": "food"
      }
    }
    ```

---

## Phase 2: Multi-Tenancy Architecture (รองรับหลายเจ้าของ)
*สถานะ: แผนงานอนาคต*

การเปลี่ยนระบบให้รองรับ User หลายคนพร้อมกัน (Data Isolation)

### 2.1 Database Restructuring
*   **MongoDB:**
    *   Collection `tenants`: เก็บข้อมูลลูกค้า (Plan, API Limit, Owner)
    *   Collection `agents`: เก็บ Config ของ AI (ชื่อ, นิสัย, เสียง, Avatar URL)
    *   Collection `knowledge_base`: ข้อมูลดิบที่อัปโหลดมา
*   **Authentication:** ใช้ JWT (JSON Web Token) เพื่อระบุตัวตนว่า Request นี้มาจาก Tenant ไหน

### 2.2 API Key Management
*   ลูกค้าจะได้รับ `API_KEY` ของตัวเอง
*   ทุก Request ที่ยิงเข้า API Gateway ต้องแนบ Key นี้มาเพื่อ Route ไปหา Knowledge Base ที่ถูกต้อง

---

## Phase 3: AI Builder Studio (หน้าบ้านสำหรับสร้าง AI)
*สถานะ: Concept Design*

เครื่องมือ No-Code สำหรับเจ้าของ AI

### 3.1 Features
*   **Knowledge Uploader:** ลากวางไฟล์ PDF เมนูอาหาร -> ระบบ Auto-Ingest ลง Qdrant
*   **Personality Tweaker:** ปรับนิสัย AI (ตลก, ทางการ, ขี้เล่น) ผ่าน Web UI (ไม่ต้องแก้ Code Prompt)
*   **Avatar Customizer:** อัปโหลดรูปแบรนด์ หรือเลือกโมเดล 3D (ในอนาคต)

### 3.2 Widget Generator
*   ปุ่ม "Get Code" เพื่อรับ JavaScript snippet ไปแปะหน้าเว็บลูกค้า
    ```html
    <script src="https://platform.com/widget.js" data-agent-id="shop_123"></script>
    ```

---

## 🗓️ Timeline Estimation

| Phase | ระยะเวลา | ความยาก | สิ่งที่ต้องเตรียม |
| :--- | :--- | :--- | :--- |
| **1. Separation** | 1 เดือน | 🟡 ปานกลาง | Refactor Code ส่วน RAG Core |
| **2. Multi-Tenancy** | 2-3 เดือน | 🔴 ยาก | ออกแบบ Database ใหม่ทั้งหมด |
| **3. AI Builder UI** | 2 เดือน | 🟡 ปานกลาง | เขียน Frontend Dashboard (React/Vue) |

---

## สรุป (Conclusion)
การไปสู่ Universal Platform ไม่ใช่แค่การเพิ่มฟีเจอร์ แต่คือการ **"รื้อโครงสร้างพื้นฐาน" (Refactoring)** ครั้งใหญ่ แนะนำให้เริ่มจาก Phase 1 (ทำให้ Code รองรับ Configurable Prompt ก่อน) แล้วค่อยขยับไปทำระบบ Login และ Database แยกใน Phase 2 ครับ
