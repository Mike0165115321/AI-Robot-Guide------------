# 📖 คู่มือการติดตั้ง AI Robot Guide - จังหวัดน่าน

## สารบัญ
- [1. ภาพรวมระบบ](#1-ภาพรวมระบบ)
- [2. ความต้องการของระบบ](#2-ความต้องการของระบบ)
- [3. การติดตั้งซอฟต์แวร์พื้นฐาน](#3-การติดตั้งซอฟต์แวร์พื้นฐาน)
- [4. การดาวน์โหลดโปรเจกต์](#4-การดาวน์โหลดโปรเจกต์)
- [5. การตั้งค่าฐานข้อมูล](#5-การตั้งค่าฐานข้อมูล)
- [6. การตั้งค่า Python Environment](#6-การตั้งค่า-python-environment)
- [7. การตั้งค่า API Keys](#7-การตั้งค่า-api-keys)
- [8. การสร้าง Vector Database](#8-การสร้าง-vector-database)
- [9. การรันระบบ](#9-การรันระบบ)
- [10. การทดสอบระบบ](#10-การทดสอบระบบ)
- [11. การแก้ไขปัญหาเบื้องต้น](#11-การแก้ไขปัญหาเบื้องต้น)

---

## 1. ภาพรวมระบบ

### 1.1. คำอธิบายโปรเจกต์
- โปรเจกต์ AI Robot Guide เป็นระบบ AI ที่ช่วยแนะนำสถานที่ท่องเที่ยวและข้อมูลสำคัญของจังหวัดน่าน

### 1.2. สถาปัตยกรรมระบบ

#### 1.2.1. ส่วนประกอบหลัก
  - (1) **Frontend** - หน้าเว็บสำหรับผู้ใช้งาน (HTML5, CSS3, JavaScript)
  - (2) **Backend** - เซิร์ฟเวอร์ API (FastAPI/Python)
  - (3) **MongoDB** - ฐานข้อมูลหลักสำหรับเก็บข้อมูลสถานที่
  - (4) **Qdrant** - ฐานข้อมูล Vector สำหรับการค้นหาอัจฉริยะ

#### 1.2.2. เทคโนโลยีที่ใช้
  - (1) **AI/LLM**: Gemini, Groq (Llama 3.x)
  - (2) **Speech-to-Text (STT)**: Whisper
  - (3) **Text-to-Speech (TTS)**: EdgeTTS, gTTS
  - (4) **RAG Pipeline**: ระบบค้นหาข้อมูลจาก Vector DB

---

## 2. ความต้องการของระบบ

### 2.1. ความต้องการฮาร์ดแวร์

#### 2.1.1. ความต้องการขั้นต่ำ
  - (1) CPU: Intel Core i5 หรือเทียบเท่า
  - (2) RAM: 8 GB ขึ้นไป
  - (3) พื้นที่เก็บข้อมูล: 20 GB ขึ้นไป
  - (4) การเชื่อมต่ออินเทอร์เน็ต

#### 2.1.2. ความต้องการที่แนะนำ (สำหรับประสิทธิภาพสูงสุด)
  - (1) CPU: Intel Core i7 หรือเทียบเท่า
  - (2) RAM: 16 GB ขึ้นไป
  - (3) GPU: NVIDIA GPU (สำหรับการใช้ Local Whisper)
  - (4) พื้นที่เก็บข้อมูล: 50 GB ขึ้นไป

### 2.2. ความต้องการซอฟต์แวร์

#### 2.2.1. ระบบปฏิบัติการที่รองรับ
  - (1) Windows 10/11
  - (2) Ubuntu 20.04 LTS ขึ้นไป
  - (3) macOS 11 ขึ้นไป

#### 2.2.2. ซอฟต์แวร์ที่ต้องติดตั้ง
  - (1) Python 3.12 ขึ้นไป
  - (2) Docker Desktop
  - (3) Git (ไม่บังคับ - สำหรับการ Clone โปรเจกต์)

---

## 3. การติดตั้งซอฟต์แวร์พื้นฐาน

### 3.1. การติดตั้ง Python

#### 3.1.1. สำหรับ Windows

##### 3.1.1.1. ดาวน์โหลด Python
  - (1) เปิดเว็บเบราว์เซอร์
  - (2) ไปที่ https://www.python.org/downloads/
  - (3) คลิกปุ่ม "Download Python 3.12.x" (หรือเวอร์ชันล่าสุด)
  - (4) รอการดาวน์โหลดเสร็จสิ้น

##### 3.1.1.2. ติดตั้ง Python
  - (1) ดับเบิลคลิกไฟล์ที่ดาวน์โหลดมา
  - (2) **สำคัญ**: ติ๊กเลือก "Add python.exe to PATH"
  - (3) คลิก "Install Now"
  - (4) รอการติดตั้งเสร็จสิ้น
  - (5) คลิก "Close" เมื่อติดตั้งเสร็จ

##### 3.1.1.3. ตรวจสอบการติดตั้ง
  - (1) เปิด Command Prompt (กด Windows + R พิมพ์ cmd แล้วกด Enter)
  - (2) พิมพ์คำสั่ง:
    ```bash
    python --version
    ```
  - (3) ควรแสดงผล: `Python 3.12.x`

#### 3.1.2. สำหรับ Linux (Ubuntu/Debian)

##### 3.1.2.1. อัปเดตระบบ
  - (1) เปิด Terminal
  - (2) รันคำสั่ง:
    ```bash
    sudo apt update && sudo apt upgrade -y
    ```

##### 3.1.2.2. ติดตั้ง Python
  - (1) รันคำสั่ง:
    ```bash
    sudo apt install python3.12 python3.12-venv python3-pip -y
    ```

##### 3.1.2.3. ตรวจสอบการติดตั้ง
  - (1) รันคำสั่ง:
    ```bash
    python3 --version
    ```
  - (2) ควรแสดงผล: `Python 3.12.x`

### 3.2. การติดตั้ง Docker Desktop

#### 3.2.1. สำหรับ Windows

##### 3.2.1.1. ดาวน์โหลด Docker Desktop
  - (1) ไปที่ https://www.docker.com/products/docker-desktop/
  - (2) คลิก "Download for Windows"
  - (3) รอการดาวน์โหลดเสร็จสิ้น

##### 3.2.1.2. ติดตั้ง Docker Desktop
  - (1) ดับเบิลคลิกไฟล์ `Docker Desktop Installer.exe`
  - (2) ทำเครื่องหมายเลือก "Use WSL 2 instead of Hyper-V"
  - (3) คลิก "OK" และรอการติดตั้ง
  - (4) รีสตาร์ทเครื่องเมื่อมีการแจ้งเตือน

##### 3.2.1.3. เริ่มใช้งาน Docker Desktop
  - (1) เปิดโปรแกรม Docker Desktop จาก Start Menu
  - (2) ยอมรับข้อตกลงการใช้งาน (Terms)
  - (3) รอจนกว่า Docker Engine จะแสดงสถานะ "Running"

##### 3.2.1.4. ตรวจสอบการติดตั้ง
  - (1) เปิด Command Prompt
  - (2) พิมพ์คำสั่ง:
    ```bash
    docker --version
    ```
  - (3) ควรแสดงผลเวอร์ชันของ Docker

#### 3.2.2. สำหรับ Linux (Ubuntu)

##### 3.2.2.1. ติดตั้ง Docker Engine
  - (1) รันคำสั่ง:
    ```bash
    curl -fsSL https://get.docker.com -o get-docker.sh
    sudo sh get-docker.sh
    ```

##### 3.2.2.2. เพิ่ม User เข้ากลุ่ม Docker
  - (1) รันคำสั่ง:
    ```bash
    sudo usermod -aG docker $USER
    ```
  - (2) ออกจากระบบและเข้าใหม่ (Log out และ Log in)

##### 3.2.2.3. ติดตั้ง Docker Compose
  - (1) รันคำสั่ง:
    ```bash
    sudo apt install docker-compose-plugin -y
    ```

##### 3.2.2.4. ตรวจสอบการติดตั้ง
  - (1) รันคำสั่ง:
    ```bash
    docker --version
    docker compose version
    ```

### 3.3. การติดตั้ง Git (ไม่บังคับ)

#### 3.3.1. สำหรับ Windows
  - (1) ไปที่ https://git-scm.com/downloads
  - (2) คลิก "Download for Windows"
  - (3) ติดตั้งโดยใช้ค่าเริ่มต้นทั้งหมด

#### 3.3.2. สำหรับ Linux
  - (1) รันคำสั่ง:
    ```bash
    sudo apt install git -y
    ```

---

## 4. การดาวน์โหลดโปรเจกต์

### 4.1. วิธีที่ 1: ดาวน์โหลดไฟล์ ZIP (แนะนำสำหรับผู้เริ่มต้น)

#### 4.1.1. ดาวน์โหลดจาก GitHub
  - (1) ไปที่ https://github.com/Mike0165115321/AI-Robot-Guide-
  - (2) คลิกปุ่ม "Code" สีเขียว
  - (3) คลิก "Download ZIP"
  - (4) รอการดาวน์โหลดเสร็จสิ้น

#### 4.1.2. แตกไฟล์
  - (1) เปิด File Explorer ไปที่โฟลเดอร์ Downloads
  - (2) คลิกขวาที่ไฟล์ `AI-Robot-Guide--main.zip`
  - (3) เลือก "Extract All..."
  - (4) เลือกตำแหน่งที่ต้องการเก็บโปรเจกต์
  - (5) คลิก "Extract"

### 4.2. วิธีที่ 2: Clone ด้วย Git

#### 4.2.1. เปิด Terminal/Command Prompt
  - (1) นำทางไปยังโฟลเดอร์ที่ต้องการเก็บโปรเจกต์:
    ```bash
    cd ~/Documents
    ```
    หรือบน Windows:
    ```bash
    cd C:\Users\YourName\Documents
    ```

#### 4.2.2. Clone โปรเจกต์
  - (1) รันคำสั่ง:
    ```bash
    git clone https://github.com/Mike0165115321/AI-Robot-Guide-.git
    ```

#### 4.2.3. เข้าสู่โฟลเดอร์โปรเจกต์
  - (1) รันคำสั่ง:
    ```bash
    cd AI-Robot-Guide-
    ```

---

## 5. การตั้งค่าฐานข้อมูล

### 5.1. เริ่มต้นใช้งาน Docker

#### 5.1.1. ตรวจสอบว่า Docker กำลังทำงาน

##### 5.1.1.1. สำหรับ Windows
  - (1) เปิดโปรแกรม Docker Desktop
  - (2) รอจนแสดงสถานะ "Docker Desktop is running"
  - (3) ตรวจสอบว่าไอคอน Docker ใน System Tray เป็นสีเขียว

##### 5.1.1.2. สำหรับ Linux
  - (1) รันคำสั่ง:
    ```bash
    sudo systemctl start docker
    sudo systemctl status docker
    ```
  - (2) ควรแสดงสถานะ "active (running)"

### 5.2. รันฐานข้อมูลด้วย Docker Compose

#### 5.2.1. เปิด Terminal ที่โฟลเดอร์โปรเจกต์

##### 5.2.1.1. สำหรับ Windows
  - (1) เปิด File Explorer ไปที่โฟลเดอร์โปรเจกต์
  - (2) คลิกขวาในพื้นที่ว่าง
  - (3) เลือก "Open in Terminal" หรือ "Open PowerShell window here"

##### 5.2.1.2. สำหรับ Linux
  - (1) คลิกขวาในโฟลเดอร์โปรเจกต์
  - (2) เลือก "Open Terminal Here"
  - (3) หรือใช้คำสั่ง `cd` เพื่อนำทาง

#### 5.2.2. รัน Docker Compose
  - (1) รันคำสั่ง:
    ```bash
    docker compose up -d mongodb qdrant
    ```
    หรือ (สำหรับ Docker Compose เวอร์ชันเก่า):
    ```bash
    docker-compose up -d mongodb qdrant
    ```
  - (2) รอจนการดาวน์โหลด images และสร้าง containers เสร็จสิ้น

#### 5.2.3. ตรวจสอบสถานะ

##### 5.2.3.1. ตรวจสอบว่า Containers กำลังทำงาน
  - (1) รันคำสั่ง:
    ```bash
    docker ps
    ```
  - (2) ควรเห็น containers ทั้ง 2 ตัว:
    - `mongodb_db` - ทำงานที่ port 27017
    - `qdrant_db` - ทำงานที่ port 6333, 6334

##### 5.2.3.2. ทดสอบการเชื่อมต่อ Qdrant
  - (1) เปิดเว็บเบราว์เซอร์
  - (2) ไปที่ http://localhost:6333/dashboard
  - (3) ควรเห็นหน้า Qdrant Dashboard

### 5.3. การหยุดและเริ่มฐานข้อมูลใหม่

#### 5.3.1. หยุดฐานข้อมูล
  - (1) รันคำสั่ง:
    ```bash
    docker compose down
    ```
  - **หมายเหตุ**: ข้อมูลจะยังคงอยู่ใน Docker volumes

#### 5.3.2. เริ่มฐานข้อมูลใหม่
  - (1) รันคำสั่ง:
    ```bash
    docker compose up -d mongodb qdrant
    ```

---

## 6. การตั้งค่า Python Environment

### 6.1. สร้าง Virtual Environment

#### 6.1.1. เปิด Terminal ที่โฟลเดอร์โปรเจกต์
  - (1) ตรวจสอบว่าอยู่ที่โฟลเดอร์หลักของโปรเจกต์ (ที่มีโฟลเดอร์ `Back-end` และ `frontend`)

#### 6.1.2. สร้าง Virtual Environment

##### 6.1.2.1. สำหรับ Windows
  - (1) รันคำสั่ง:
    ```bash
    python -m venv .venv-robot
    ```

##### 6.1.2.2. สำหรับ Linux/macOS
  - (1) รันคำสั่ง:
    ```bash
    python3 -m venv .venv-robot
    ```

### 6.2. เปิดใช้งาน Virtual Environment

#### 6.2.1. สำหรับ Windows (Command Prompt)
  - (1) รันคำสั่ง:
    ```bash
    .venv-robot\Scripts\activate
    ```

#### 6.2.2. สำหรับ Windows (PowerShell)
  - (1) รันคำสั่ง:
    ```bash
    .venv-robot\Scripts\Activate.ps1
    ```
  - **หมายเหตุ**: หากมี error เรื่อง execution policy ให้รัน:
    ```bash
    Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
    ```

#### 6.2.3. สำหรับ Linux/macOS
  - (1) รันคำสั่ง:
    ```bash
    source .venv-robot/bin/activate
    ```

#### 6.2.4. ตรวจสอบว่า Virtual Environment ถูกเปิดใช้งาน
  - (1) ดูที่ prompt ของ Terminal
  - (2) ควรเห็น `(.venv-robot)` นำหน้า prompt
  - (3) ตัวอย่าง:
    ```
    (.venv-robot) C:\Users\YourName\AI-Robot-Guide->
    ```
    หรือ
    ```
    (.venv-robot) user@computer:~/AI-Robot-Guide-$
    ```

### 6.3. ติดตั้ง Dependencies

#### 6.3.1. อัปเดต pip
  - (1) รันคำสั่ง:
    ```bash
    pip install --upgrade pip
    ```

#### 6.3.2. ติดตั้ง packages ทั้งหมด
  - (1) รันคำสั่ง:
    ```bash
    pip install -r Back-end/requirements.txt
    ```
  - (2) รอการติดตั้ง (อาจใช้เวลา 5-15 นาที ขึ้นอยู่กับความเร็วอินเทอร์เน็ต)

#### 6.3.3. ตรวจสอบการติดตั้ง
  - (1) รันคำสั่ง:
    ```bash
    pip list
    ```
  - (2) ควรเห็นรายการ packages ที่ติดตั้งแล้ว

---

## 7. การตั้งค่า API Keys

### 7.1. สร้างไฟล์ .env

#### 7.1.1. สร้างไฟล์ .env ใหม่

##### 7.1.1.1. วิธีที่ 1: คัดลอกจากไฟล์ตัวอย่าง
  - (1) ไปที่โฟลเดอร์ `Back-end`
  - (2) คัดลอกไฟล์ `.env.example` เป็น `.env`:
    - **Windows**:
      ```bash
      copy Back-end\.env.example Back-end\.env
      ```
    - **Linux/macOS**:
      ```bash
      cp Back-end/.env.example Back-end/.env
      ```

##### 7.1.1.2. วิธีที่ 2: สร้างไฟล์ใหม่
  - (1) เปิด Text Editor (Notepad, VS Code, etc.)
  - (2) สร้างไฟล์ใหม่ชื่อ `.env` ในโฟลเดอร์ `Back-end`

### 7.2. ขอ API Keys

#### 7.2.1. GEMINI_API_KEYS (Gemini AI)

##### 7.2.1.1. สร้าง API Key
  - (1) ไปที่ https://aistudio.google.com/app/apikey
  - (2) ลงชื่อเข้าใช้ด้วย Google Account
  - (3) คลิก "Create API Key"
  - (4) เลือก Project หรือสร้าง Project ใหม่
  - (5) คัดลอก API Key ที่ได้

##### 7.2.1.2. หมายเหตุ
  - สามารถสร้างได้หลาย Key และคั่นด้วยเครื่องหมายจุลภาค `,`
  - ตัวอย่าง: `AIzaSy...key1,AIzaSy...key2`

#### 7.2.2. GROQ_API_KEYS (Groq AI)

##### 7.2.2.1. สร้าง API Key
  - (1) ไปที่ https://console.groq.com/keys
  - (2) ลงทะเบียน/ลงชื่อเข้าใช้
  - (3) คลิก "Create API Key"
  - (4) ตั้งชื่อ Key
  - (5) คัดลอก API Key ที่ได้

##### 7.2.2.2. หมายเหตุ
  - สามารถสร้างได้หลาย Key และคั่นด้วยเครื่องหมายจุลภาค `,`
  - ตัวอย่าง: `gsk_...key1,gsk_...key2`

#### 7.2.3. YOUTUBE_API_KEY และ GOOGLE_API_KEY

##### 7.2.3.1. เปิดใช้งาน API
  - (1) ไปที่ https://console.cloud.google.com/
  - (2) สร้าง Project ใหม่ หรือเลือก Project ที่มีอยู่
  - (3) ไปที่ "APIs & Services" > "Library"
  - (4) ค้นหาและเปิดใช้งาน:
    - "YouTube Data API v3"
    - "Custom Search API"

##### 7.2.3.2. สร้าง API Key
  - (1) ไปที่ https://console.cloud.google.com/apis/credentials
  - (2) คลิก "+ CREATE CREDENTIALS"
  - (3) เลือก "API key"
  - (4) คัดลอก API Key ที่ได้

#### 7.2.4. GOOGLE_CSE_ID (Custom Search Engine ID)

##### 7.2.4.1. สร้าง Custom Search Engine
  - (1) ไปที่ https://programmablesearchengine.google.com/controlpanel/all
  - (2) คลิก "Add" หรือ "New search engine"
  - (3) ใส่ข้อมูลที่ต้องการ
  - (4) คลิก "Create"

##### 7.2.4.2. รับ Search Engine ID
  - (1) ไปที่ Search Engine ที่สร้าง
  - (2) คลิก "Setup"
  - (3) ดูที่ส่วน "Basic" > "Search engine ID"
  - (4) คัดลอก ID

### 7.3. กรอกข้อมูลในไฟล์ .env

#### 7.3.1. โครงสร้างไฟล์ .env ที่สมบูรณ์
  - (1) เปิดไฟล์ `Back-end/.env` ด้วย Text Editor
  - (2) กรอกข้อมูลตามรูปแบบนี้:

```env
# Database Configuration
MONGO_URI=mongodb://localhost:27017/
MONGO_DATABASE_NAME=nanaiguide

# AI Model Configuration
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-large
RERANKER_MODEL_NAME=BAAI/bge-reranker-base

# API Keys
GEMINI_API_KEYS=AIzaSy...your_key_1,AIzaSy...your_key_2
GROQ_API_KEYS=gsk_...your_key_1,gsk_...your_key_2
GOOGLE_API_KEY=your_google_api_key
GOOGLE_CSE_ID=your_google_cse_id
YOUTUBE_API_KEY=your_youtube_api_key

# Vector DB
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Server Config
API_HOST=0.0.0.0
API_PORT=9090
```

#### 7.3.2. ข้อควรระวัง
  - (1) ห้ามมีช่องว่างก่อนหรือหลังเครื่องหมาย `=`
  - (2) ไม่ต้องใส่ค่าในเครื่องหมายคำพูด (ยกเว้นมีอักขระพิเศษ)
  - (3) ต้องใช้ localhost หรือ 127.0.0.1 สำหรับ MONGO_URI และ QDRANT_HOST

---

## 8. การสร้าง Vector Database

### 8.1. เตรียมความพร้อม

#### 8.1.1. ตรวจสอบ Prerequisites

##### 8.1.1.1. ตรวจสอบว่า Docker containers กำลังทำงาน
  - (1) รันคำสั่ง:
    ```bash
    docker ps
    ```
  - (2) ต้องเห็น `mongodb_db` และ `qdrant_db` อยู่ในรายการ

##### 8.1.1.2. ตรวจสอบว่า Virtual Environment ถูกเปิดใช้งาน
  - (1) ดูว่ามี `(.venv-robot)` นำหน้า prompt

##### 8.1.1.3. ตรวจสอบว่าไฟล์ .env ถูกต้อง
  - (1) ตรวจสอบว่า API Keys ถูกกรอกครบถ้วน

### 8.2. รันสคริปต์เตรียมข้อมูล

#### 8.2.1. เพิ่มลิงก์รูปภาพ

##### 8.2.1.1. รันคำสั่ง
  - (1) รันคำสั่ง:
    - **Windows**:
      ```bash
      python Back-end/utils/add_image_links.py
      ```
    - **Linux/macOS**:
      ```bash
      python3 Back-end/utils/add_image_links.py
      ```
  - (2) รอจนสคริปต์ทำงานเสร็จ

##### 8.2.1.2. ตรวจสอบผลลัพธ์
  - (1) ควรเห็นข้อความแสดงความสำเร็จใน Terminal

### 8.3. สร้าง Vector Database

#### 8.3.1. รันสคริปต์สร้าง Vectors

##### 8.3.1.1. รันคำสั่ง
  - (1) รันคำสั่ง:
    - **Windows**:
      ```bash
      python Back-end/scripts/build_vectors.py
      ```
    - **Linux/macOS**:
      ```bash
      python3 Back-end/scripts/build_vectors.py
      ```
  - (2) รอจนสคริปต์ทำงานเสร็จ (อาจใช้เวลา 5-30 นาที ครั้งแรก)

##### 8.3.1.2. สิ่งที่จะเกิดขึ้น
  - (1) ดาวน์โหลด Embedding Model (ครั้งแรก ~2GB)
  - (2) อ่านข้อมูลจากไฟล์ JSONL
  - (3) สร้าง Vector Embeddings
  - (4) บันทึกลง MongoDB และ Qdrant

##### 8.3.1.3. ตรวจสอบผลลัพธ์
  - (1) ควรเห็นข้อความ "Vector database built successfully" หรือคล้ายกัน
  - (2) สามารถตรวจสอบใน Qdrant Dashboard ที่ http://localhost:6333/dashboard

### 8.4. การล้างและสร้างฐานข้อมูลใหม่ (ถ้าจำเป็น)

#### 8.4.1. ล้างฐานข้อมูล

> ⚠️ **คำเตือน**: การรันคำสั่งนี้จะลบข้อมูลทั้งหมดในฐานข้อมูล!

##### 8.4.1.1. รันคำสั่งล้างฐานข้อมูล
  - (1) รันคำสั่ง:
    - **Windows**:
      ```bash
      python Back-end/scripts/clear_database.py
      ```
    - **Linux/macOS**:
      ```bash
      python3 Back-end/scripts/clear_database.py
      ```

##### 8.4.1.2. สร้างฐานข้อมูลใหม่
  - (1) กลับไปทำขั้นตอน 8.2 และ 8.3 อีกครั้ง

---

## 9. การรันระบบ

### 9.1. วิธีที่แนะนำ (Automatic)

#### 9.1.1. ใช้สคริปต์ start_all.sh
  - (1) **Linux/macOS**:
    ```bash
    chmod +x start_all.sh
    ./start_all.sh
    ```
  - (2) **Windows**: แนะนำให้รันผ่าน WSL 2 หรือ Git Bash

### 9.2. วิธี Manual (ทีละขั้นตอน)

#### 9.2.1. ขั้นตอนที่ 1: รันฐานข้อมูล
  - (1) รันคำสั่ง:
    ```bash
    docker compose up -d mongodb qdrant
    ```

#### 9.2.2. ขั้นตอนที่ 2: รัน Python Backend
  - (1) เปิด Terminal ใหม่
  - (2) เข้าโฟลเดอร์: `cd Back-end`
  - (3) Activate venv: `source ../.venv-robot/bin/activate`
  - (4) รัน: `uvicorn api.main:app --host 127.0.0.1 --port 9090 --reload`

### 9.3. การใช้งาน
  - (1) เปิดเบราว์เซอร์ไปที่: `http://localhost:9090`
  - (2) สามารถใช้งานหน้า Chat และ Avatar ได้ทันที

### 9.4. การหยุดระบบ
  - (1) กด `Ctrl + C` ในทุก Terminal
  - (2) รัน `docker compose down` เพื่อปิดฐานข้อมูล

---

## 10. การทดสอบระบบ

### 10.1. ทดสอบ Backend API

#### 10.1.1. ทดสอบ Health Check
  - (1) เปิดเว็บเบราว์เซอร์
  - (2) ไปที่ http://127.0.0.1:9090/health
  - (3) ควรได้ response: `{"status": "ok"}` หรือคล้ายกัน

#### 10.1.2. ทดสอบ API Documentation
  - (1) ไปที่ http://127.0.0.1:9090/docs
  - (2) ลองใช้งาน Endpoint ต่างๆ ผ่าน Swagger UI

### 10.2. ทดสอบ Frontend

#### 10.2.1. ทดสอบหน้า Chat
  - (1) เปิดไฟล์ `frontend/chat.html`
  - (2) ลองพิมพ์ข้อความถามเกี่ยวกับสถานที่ท่องเที่ยวน่าน
  - (3) ควรได้รับคำตอบจาก AI

#### 10.2.2. ทดสอบหน้า Avatar
  - (1) เปิดไฟล์ `frontend/robot_avatar.html`
  - (2) ลองใช้งาน Wake Word "น้องน่าน"
  - (3) ทดสอบการพูดคุยผ่านไมโครโฟน

### 10.3. ทดสอบฐานข้อมูล

#### 10.3.1. ตรวจสอบ MongoDB
  - (1) รันคำสั่ง:
    ```bash
    docker exec -it mongodb_db mongosh
    ```
  - (2) พิมพ์คำสั่ง:
    ```
    show dbs
    use nanaiguide
    show collections
    ```
  - (3) พิมพ์ `exit` เพื่อออก

#### 10.3.2. ตรวจสอบ Qdrant
  - (1) เปิด http://localhost:6333/dashboard
  - (2) ตรวจสอบว่ามี Collections และข้อมูลอยู่

---

## 11. การแก้ไขปัญหาเบื้องต้น

### 11.1. ปัญหา Docker

#### 11.1.1. Docker ไม่ทำงาน

##### 11.1.1.1. สาเหตุที่เป็นไปได้
  - (1) Docker Desktop ไม่ได้เปิด
  - (2) WSL 2 ไม่ได้ติดตั้ง (Windows)
  - (3) Docker service ไม่ได้เริ่มทำงาน

##### 11.1.1.2. วิธีแก้ไข
  - (1) เปิด Docker Desktop และรอจนแสดงสถานะ Running
  - (2) รีสตาร์ท Docker Desktop
  - (3) สำหรับ Linux รัน: `sudo systemctl restart docker`

#### 11.1.2. Port ถูกใช้งานแล้ว

##### 11.1.2.1. ข้อผิดพลาด
  - (1) `Error: Port 27017 (or 6333) is already in use`

##### 11.1.2.2. วิธีแก้ไข
  - (1) หยุดโปรแกรมที่ใช้ port นั้น
  - (2) หรือแก้ไข port ในไฟล์ `docker-compose.yml`

### 11.2. ปัญหา Python

#### 11.2.1. ModuleNotFoundError

##### 11.2.1.1. สาเหตุ
  - (1) ไม่ได้เปิด Virtual Environment
  - (2) ยังไม่ได้ติดตั้ง packages

##### 11.2.1.2. วิธีแก้ไข
  - (1) ตรวจสอบว่ามี `(.venv-robot)` นำหน้า prompt
  - (2) รัน: `pip install -r Back-end/requirements.txt`

#### 11.2.2. Permission Error

##### 11.2.2.1. วิธีแก้ไข (Linux)
  - (1) ใช้ `sudo` นำหน้าคำสั่ง (ไม่แนะนำ)
  - (2) หรือใช้ `--user` flag:
    ```bash
    pip install --user -r Back-end/requirements.txt
    ```

### 11.3. ปัญหา API Keys

#### 11.3.1. API Key ไม่ถูกต้อง

##### 11.3.1.1. อาการ
  - (1) Error: "Invalid API key"
  - (2) Response: 401 Unauthorized

##### 11.3.1.2. วิธีแก้ไข
  - (1) ตรวจสอบว่า API Key ถูกคัดลอกมาครบถ้วน
  - (2) ตรวจสอบว่าไม่มีช่องว่างก่อน/หลัง Key
  - (3) สร้าง Key ใหม่หากจำเป็น

### 11.4. ปัญหาการเชื่อมต่อฐานข้อมูล

#### 11.4.1. ไม่สามารถเชื่อมต่อ MongoDB/Qdrant

##### 11.4.1.1. สาเหตุที่เป็นไปได้
  - (1) Docker containers ไม่ได้ทำงาน
  - (2) MONGO_URI หรือ QDRANT_HOST ไม่ถูกต้อง

##### 11.4.1.2. วิธีแก้ไข
  - (1) รัน `docker ps` เพื่อตรวจสอบ containers
  - (2) ตรวจสอบค่าในไฟล์ `.env`:
    - `MONGO_URI=mongodb://localhost:27017/`
    - `QDRANT_HOST=localhost`

### 11.5. การขอความช่วยเหลือ

#### 11.5.1. แหล่งข้อมูลเพิ่มเติม
  - (1) อ่านไฟล์ README.md ในโปรเจกต์
  - (2) ตรวจสอบ Issues บน GitHub
  - (3) ติดต่อทีมพัฒนา

---

## 📞 ข้อมูลติดต่อ

หากพบปัญหาในการติดตั้งหรือต้องการความช่วยเหลือ สามารถติดต่อได้ที่:
- GitHub Issues: https://github.com/Mike0165115321/AI-Robot-Guide-/issues

---

**เวอร์ชัน**: 1.0  
**อัปเดตล่าสุด**: ธันวาคม 2024
